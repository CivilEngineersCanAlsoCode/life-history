# Capability: Centralized lists / Fundamental Assessments/Notable Lists

**BRD Ref:** 12.6

## Business Requirement
System should have the capability to setup and maintain all reference lists

## Purpose
Capability to setup reference lists at multiple levels and have the ability to override locally

## Outcome
Efficient and flexible system allowing for simpler implementation of reference lists


## Discovery Questions
- [ ] How do we handle 'copy-on-write' for Centralized Lists when a market edits a shared list?
- [ ] Should changes to centralized lists auto-propagate to all markets or require acceptance?
- [ ] Do we need a 'Dictionary' view to see all lists and their usage across rules?

## Development Status

**Status:** No
**Comments:** In Development (Integration Target 26.3)


## System Behavior & User Journey

**System Behavior:** The Asset Manager acts as the CRUD interface for all Reference Data (Lists, Industry Codes, Country Scores). Data is stored in normalized relational tables. The UI provides filtering, bulk upload (Excel/CSV), and version history for these lists. Changes here propagate to the Rule Execution Engine via cache invalidation or batch updates.

**User Journey:** Navigation -> Asset Manager -> Select List (e.g., 'High Risk Geographies') -> 'Edit' or 'Upload New Version' -> Validate Entries -> Save.


## Business Questions (Mom Test)

These questions are designed to reveal hidden desires and operational realities:

- What is the single most expensive mistake a user could make here?
- How do you currently solve this problem manually (Excel? Email?)?
- How frequently does this list data actually change? Daily? Yearly?
