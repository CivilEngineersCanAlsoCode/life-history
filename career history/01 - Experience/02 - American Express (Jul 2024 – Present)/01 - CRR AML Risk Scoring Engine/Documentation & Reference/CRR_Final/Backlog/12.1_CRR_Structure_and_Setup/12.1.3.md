# Sub-Requirement: 12.1.3

**Parent Capability:** [CRR Structure and Setup](12.1_Capability.md)

## Description
The multiplier of a risk element is set based on rule criteria.
   The system should have ability to have multiple rulesets.
   The system should have ability to combine data points to establish criteria.
   For each element users should be able to identify minimum or maximum multiplier
       For example, minimum for Length of Relationship and Acquisition Channel
       Maximum is set for all others.  Potentially this is the default.
   System should have the ability to setup rule to trigger based on fundamental assessment risk score (1 to 10) / Centralized Lists
       For example, In 7,8,9 for high risk geographies
   System should have the ability to setup a default multiplier (to be leveraged when data is unavailable)


## Discovery Questions
- [ ] Do we need version control for individual Risk Elements?
- [ ] What happens to historical scores if a Risk Element is renamed?

## Development Status

**Status:** No
**Comments:** In Development (Target 26.3 for full Integration)


## System Behavior & User Journey

**System Behavior:** Users access the 'Sandbox' module via the React Frontend. When a new simulation is initialized, the backend (Python/Node) copies the active Production configuration (Rulesets, Weights, Multipliers) into a temporary 'Draft' schema in the database. Users then modify this draft configuration. The Rule Execution Engine (running in Simulation Mode) scores a sample population against this draft config to generate impact metrics.

**User Journey:** Login -> Dashboard -> Sandbox -> 'Create New Simulation' -> Select Scope (Enterprise/Market) -> Modify Risk Weights -> Run Simulation -> View Impact -> Submit for Approval.


## Business Questions (Mom Test)

These questions are designed to reveal hidden desires and operational realities:

- What is the single most expensive mistake a user could make here?
- How do you currently solve this problem manually (Excel? Email?)?
- If a simulation takes 4 hours, what do you do while waiting?
